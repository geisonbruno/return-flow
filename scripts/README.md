# Scripts

Reserved for future cross-app automation (e.g. bootstrap, release, or CI helper scripts).

No scripts are needed yet — each app builds, lints, and tests independently through its own package manager (see the root `README.md`).
