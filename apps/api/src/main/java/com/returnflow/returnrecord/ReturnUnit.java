package com.returnflow.returnrecord;

/** The two MVP units of measure a return quantity can be recorded in (root CLAUDE.md §11.3): CTN (Carton) and EA (Each). */
public enum ReturnUnit {
	CTN,
	EA
}
