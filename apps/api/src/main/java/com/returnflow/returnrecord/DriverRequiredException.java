package com.returnflow.returnrecord;

/** Thrown when the user passed as the driver of a new return is not a {@code DRIVER} (e.g. an {@code ADMIN}). */
class DriverRequiredException extends RuntimeException {
}
